import React from 'react';

export default function CallToAction(props) {
    return (
        <section>
            Pick a topic, read, write and post. 
            (Explain more here.)
       </section>
    );
}